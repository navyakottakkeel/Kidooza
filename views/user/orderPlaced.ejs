<%- include("../../views/partials/user/header") %>

<style>
  body {
    background: #fff;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .order-success-card {
    width: 100%;
    max-width: 650px;
    background: white;
    border-radius: 20px;
    padding: 50px 40px;
    text-align: center;
    box-shadow: 0 10px 35px rgba(0, 0, 0, 0.15);
    animation: fadeInUp 0.8s ease-in-out;
  }

  .success-icon {
    width: 110px;
    height: 110px;
    margin-bottom: 25px;
    background: #eafbea;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-left: auto;
    margin-right: auto;
    animation: popScale 0.7s ease-in-out;
  }

  .success-icon i {
    font-size: 55px;
    color: #28a745;
  }

  .order-success-card h2 {
    color: #28a745;
    font-weight: bold;
    margin-bottom: 15px;
    font-size: 28px;
  }

  .order-success-card p {
    color: #555;
    font-size: 18px;
    margin-bottom: 25px;
  }

  .btn {
    border-radius: 30px;
    font-size: 16px;
    padding: 10px 25px;
  }

  @keyframes fadeInUp {
    from {
      transform: translateY(30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes popScale {
    from {
      transform: scale(0.5);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
</style>

<div class="order-success-card" style="margin-top: 80px;">
  <div class="success-icon">
    <i class="fas fa-check"></i>
  </div>

  <h2>🎉 Order Placed Successfully!</h2>
  <p>Thank you for shopping with us. We’ll notify you once your order is shipped.</p>

  <div class="mt-4">
    <a href="/orders" class="btn btn-primary px-4 me-3">
      <i class="fas fa-box"></i> View Orders
    </a>
    <a href="/boys" class="btn btn-outline-success px-4">
      <i class="fas fa-shopping-bag"></i> Continue Shopping
    </a>
  </div>
</div>

<!-- Confetti Effect -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.3/dist/confetti.browser.min.js"></script>
<script>
  // Fire confetti when page loads
  window.onload = () => {
    const duration = 3 * 1000;
    const end = Date.now() + duration;

    (function frame() {
      confetti({
        particleCount: 5,
        angle: 60,
        spread: 55,
        origin: { x: 0 }
      });
      confetti({
        particleCount: 5,
        angle: 120,
        spread: 55,
        origin: { x: 1 }
      });

      if (Date.now() < end) {
        requestAnimationFrame(frame);
      }
    })();
  };
</script>

